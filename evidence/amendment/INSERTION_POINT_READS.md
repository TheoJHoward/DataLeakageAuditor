# THE SIXTEEN INSERTION POINT READS — v30a

**What this is (D3.2).** Not a count. This is the evidence itself: every `**INSERTION POINT.**`
field in `SCHEMA_SET_FINAL.md` PART 1, quoted verbatim, with every operative target it names and
whether that target resolves inside the source of record. **A later reader checks this record
instead of re-running an instrument that has already failed twice.**

**Source of record.** `SCHEMA_SET_FINAL.md`, sha256 `c8d9c22a4cd08107ab8dd4a9a908efea19c03208a8409ba853d7563f53e7d5b1`.
Verbatim text below was lifted from it at generation time, never retyped.

**Known-positive validation travels with this result (D3.3).** The sweep whose clean result this
record carries was run against the **pre-move source**, `SCHEMA_SET_FINAL.md` sha256
`8d7efb128f34969ff050647ca4e32b06a74d22ef50e2057ebb9d2fb9248f00e8` — the R79 pair's source, in
which SC-12's `§7.7` pointer text lived in `Y3_WAIVED_ENTRY_CONDITION.md` §6.3 and not in the
source of record. **It FIRED on SC-12 → Y3.** A clean result from a sweep that has not fired on a
known positive is not reported (B5.3, extended at C3).

**What this record does NOT cover (H-L21 — an instrument's PASS is a statement about its domain).**
The question answered here is keyed on `**INSERTION POINT.**` fields. Operative text that never had
such a field is **outside this record's domain and outside the instrument's**: `§10.1-C2op`
name their target in their own headings, so no field-keyed question can reach them. They were
found by reasoning forward from the SOURCE rather than from the record set, and **they are now
CLOSED**: ruled Option A at R87/§108, two records authored, regenerated, all six checks re-run. The
question is now asked as a standing detector, **D14**, which requires every normative block in the
source to be reachable by at least one record. **Sixteen of sixteen clean here was true and was
never the whole question — that is why D14 exists.**

---

## SC-1 — INSERTION POINT field at `SCHEMA_SET_FINAL.md` line 142

**Verbatim:**

```
**INSERTION POINT.** New **§2.9**, inserted after `PREREG.md` **line 266** (the closing sentence of
§2.8, "Therefore: every L2a and L3.1 finding prints its declaration…"), before the `---` at line 268.
It sits at the end of §2 because §2 is where the availability primitive is registered, and because
§2.8 already concedes that the declaration is an assumption the tool cannot verify — SC-1 is what
that concession requires in return.

```

**Targets named, and whether each resolves inside `SCHEMA_SET_FINAL.md`:**

| `PREREG.md` line | operative target? | resolves | how |
|---|---|---|---|
| 266 | **yes** | **YES** | record `SC-1`, text at ssf 161–203 |
| 268 | no — boundary | n/a | field says: "before the `---` at line 268" |

## SC-2 — INSERTION POINT field at `SCHEMA_SET_FINAL.md` line 220

**Verbatim:**

```
**INSERTION POINT.** After `PREREG.md` **line 451** (`- **Sliced variant** for CI, …`), i.e.
immediately after §6.2's bulleted element list and before the `**Pass gate — discrimination, not
tier.**` heading at line 453. This keeps the fixture's *composition* together and above the criteria
that read it.

```

**Targets named, and whether each resolves inside `SCHEMA_SET_FINAL.md`:**

| `PREREG.md` line | operative target? | resolves | how |
|---|---|---|---|
| 451 | **yes** | **YES** | record `SC-2`, text at ssf 234–270 |
| 453 | no — boundary | n/a | field says: "before the `**Pass gate - discrimination, not tier.**` heading at line 453" |

## SC-3 — INSERTION POINT field at `SCHEMA_SET_FINAL.md` line 286

**Verbatim:**

```
**INSERTION POINT.** **REPLACES `PREREG.md` line 461** (criterion 3), carrying H1 hunk **H5**'s
structure. The generic cell-key formulation below **supersedes H5's** on one axis: H5 wrote "per
declared map cell — the cell key is the unit the declaration declares the fixture to be partitioned
into"; SC-3 keeps that and adds the *checkability* and *coverage* limbs H5 left to §A.

```

**Targets named, and whether each resolves inside `SCHEMA_SET_FINAL.md`:**

| `PREREG.md` line | operative target? | resolves | how |
|---|---|---|---|
| 461 | **yes** | **YES** | record `SC-3`, text at ssf 303–359 |

## SC-4 — INSERTION POINT field at `SCHEMA_SET_FINAL.md` line 375

**Verbatim:**

```
**INSERTION POINT.** After `PREREG.md` **line 464** (`Secondary findings on **manifest-listed
descendants** …`), carrying H1 hunk **H6**'s placement. Criterion 1's own text at line 459 **stands
byte-exact**; this clause states how its denominator is constituted, which the registered text left to
be inferred.

```

**Targets named, and whether each resolves inside `SCHEMA_SET_FINAL.md`:**

| `PREREG.md` line | operative target? | resolves | how |
|---|---|---|---|
| 459 | no — explicit non-supersession | n/a | field says: "Criterion 1's own text at line 459 **stands byte-exact**" |
| 464 | **yes** | **YES** | record `SC-4`, `SC-5`, text at ssf 410–571; ssf 596–637 |

## SC-5 — INSERTION POINT field at `SCHEMA_SET_FINAL.md` line 590

**Verbatim:**

```
**INSERTION POINT.** After the block SC-4 inserts (i.e. after `PREREG.md` line 464 + SC-4), before
line 466's parenthetical. Routing must sit below the partition it routes over.

```

**Targets named, and whether each resolves inside `SCHEMA_SET_FINAL.md`:**

| `PREREG.md` line | operative target? | resolves | how |
|---|---|---|---|
| 464 | **yes** | **YES** | record `SC-4`, `SC-5`, text at ssf 410–571; ssf 596–637 |
| 466 | no — boundary | n/a | field says: "before line 466's parenthetical" |

## SC-6 — INSERTION POINT field at `SCHEMA_SET_FINAL.md` line 652

**Verbatim:**

```
**INSERTION POINT.** **Two, and both are required** (see K1 §0):

1. **`PREREG.md` line 855**, the §7.7 coverage-state table row — REPLACE the row to add the state,
   then INSERT the semantics block after the table (line 856).
2. **`PREREG.md` line 915**, §8.2 — INSERT after the sentence "None may be displayed in a way
   mistakable for a pass.", which is the existing hook; where marker M2 below is already placed at that
   site, the insertion follows M2. **Operative text: the INSERTION TEXT block below (S2(i), drafted this
   pass; Part 3).**
```

**Targets named, and whether each resolves inside `SCHEMA_SET_FINAL.md`:**

| `PREREG.md` line | operative target? | resolves | how |
|---|---|---|---|
| 855 | **yes** | **YES** | record `SC-6a`, text at ssf 682–713 |
| 856 | **yes** | **YES** | record `SC-12p`, text at ssf 1133–1140 |
| 915 | **yes** | **YES** | record `SC-6b`, text at ssf 673–681 |

## SC-7 — INSERTION POINT field at `SCHEMA_SET_FINAL.md` line 734

**Verbatim:**

```
**INSERTION POINT.** After `PREREG.md` **line 468** (`Top-k presence does not satisfy criterion 1. An
alias satisfies it only if recorded before the run.`), before line 470's "What this gate does and does
not guarantee". The surface belongs with the gate's framing, immediately after the criteria and before
the guarantee paragraph that characterizes them.

```

**Targets named, and whether each resolves inside `SCHEMA_SET_FINAL.md`:**

| `PREREG.md` line | operative target? | resolves | how |
|---|---|---|---|
| 468 | **yes** | **YES** | record `SC-7`, text at ssf 742–765 |
| 470 | no — boundary | n/a | field says: "before line 470's "What this gate does and does not guarantee"" |

## SC-8 — INSERTION POINT field at `SCHEMA_SET_FINAL.md` line 778

**Verbatim:**

```
**INSERTION POINT.** **Two:**

1. After `PREREG.md` **line 480** (`**Ordering, locked:** tune … → run the fixture gate → tag …`) —
   the freeze is the ordering rule's enforcement mechanism and belongs beside it.
2. **§11 item 8**, added after line 1054 (item 7), so registration integrity indexes the freeze **and
   carries the hash-count rule** (R23; H-L13 open form) — text in the INSERTION TEXT block below (S2(ii),
   drafted this pass; Part 3). Two supersession markers accompany it: one beneath §11's list (item 3's
   file set), one after `PREREG.md` line 97 (§0.2.1's "both").
```

**Targets named, and whether each resolves inside `SCHEMA_SET_FINAL.md`:**

| `PREREG.md` line | operative target? | resolves | how |
|---|---|---|---|
| 97 | no — supersession marker, not applied text | n/a | field says: "Two supersession markers accompany it: one beneath §11's list (item 3's file set), one after `PREREG.md` line 97 (§0.2.1's "both")" |
| 480 | **yes** | **YES** | record `SC-8a`, text at ssf 823–862 |
| 1054 | **yes** | **YES** | record `SC-8b`, text at ssf 806–822 |

## SC-9 — INSERTION POINT field at `SCHEMA_SET_FINAL.md` line 878

**Verbatim:**

```
**INSERTION POINT.** After `PREREG.md` **line 99** (the class C "discovered after the affected detector
already exists" paragraph), inside §0.2.1, before line 101's "Membership in A or B must be citable".
§0.2.1 is the amendment machinery; these are its integrity rules.

```

**Targets named, and whether each resolves inside `SCHEMA_SET_FINAL.md`:**

| `PREREG.md` line | operative target? | resolves | how |
|---|---|---|---|
| 99 | **yes** | **YES** | record `SC-9`, text at ssf 885–927 |
| 101 | no — boundary | n/a | field says: "before line 101's "Membership in A or B must be citable"" |

## SC-10 — INSERTION POINT field at `SCHEMA_SET_FINAL.md` line 943

**Verbatim:**

```
**INSERTION POINT.** After `PREREG.md` **line 441** (the closing sentence of §6.1, "The descriptive
proof count of §6.2 is the sole reported fixture outcome…"), immediately below the five-bodies table.
**See K1 finding F-5: §6.1's table is a closed enumeration headed "Five bodies of data", and this
clause collides with it unless the heading or the table is amended.**

```

**Targets named, and whether each resolves inside `SCHEMA_SET_FINAL.md`:**

| `PREREG.md` line | operative target? | resolves | how |
|---|---|---|---|
| 441 | **yes** | **YES** | record `SC-10`, text at ssf 956–986 |

## SC-11 — INSERTION POINT field at `SCHEMA_SET_FINAL.md` line 1001

**Verbatim:**

```
**INSERTION POINT.** After `PREREG.md` **line 892** (§7.8's closing paragraph, "Conformance cases
contribute to no detection metric…"), as a new §7.8 sub-block; plus a one-line pointer after **line
961** (§8.6) so the reporting section indexes it — text in the INSERTION TEXT block below (S2(iii),
drafted this pass; Part 3).

```

**Targets named, and whether each resolves inside `SCHEMA_SET_FINAL.md`:**

| `PREREG.md` line | operative target? | resolves | how |
|---|---|---|---|
| 892 | **yes** | **YES** | record `SC-11a`, text at ssf 1016–1056 |
| 961 | **yes** | **YES** | record `SC-11b`, text at ssf 1010–1015 |

## SC-12 — INSERTION POINT field at `SCHEMA_SET_FINAL.md` line 1074

**Verbatim:**

```
**INSERTION POINT.** After `PREREG.md` **line 1035** (`The replacement may be stricter than the floor
and may not be weaker: …`), preserving the three-space indentation of §10.2 criterion 2's block; plus
the §7.7 pointer after line 856 — **redrafted at DELTA R35/B3; H1's H8 draft is SUPERSEDED** and may not be applied, because it asserts that the entry condition for the `waived` coverage state is not defined by this registration, which SC-12(w) makes false. The operative pointer text is Y3 §6.3's.

```

**Targets named, and whether each resolves inside `SCHEMA_SET_FINAL.md`:**

| `PREREG.md` line | operative target? | resolves | how |
|---|---|---|---|
| 856 | **yes** | **YES** | record `SC-12p`, text at ssf 1133–1140 |
| 1035 | **yes** | **YES** | record `SC-12`, `SC-13b`, `SC-13c-1`, text at ssf 1081–1126; ssf 1376–1446; ssf 1481–1584 |

## SC-12 — INSERTION POINT field at `SCHEMA_SET_FINAL.md` line 1202

**Verbatim:**

```
**INSERTION POINT.** `PREREG.md` **line 929**, §8.3's third assertion bullet. Full-line match,
count asserted 1.

```

**Targets named, and whether each resolves inside `SCHEMA_SET_FINAL.md`:**

| `PREREG.md` line | operative target? | resolves | how |
|---|---|---|---|
| 929 | **yes** | **YES** | record `SC-12w`, text at ssf 1214–1217 |

## SC-13a — INSERTION POINT field at `SCHEMA_SET_FINAL.md` line 1247

**Verbatim:**

```
**INSERTION POINT.** **REPLACES `PREREG.md` line 1030**, the operative sentence of §10.2
criterion 2, preserving the enumeration's `2.` marker and the three-space indentation of the
continuation block. **Lines 1031, 1033 and 1035 stand byte-exact and are NOT superseded** — line
1031 is the branch sentence that authorises the replacement, line 1033 is the obligation being
discharged, line 1035 is the floor the replacement is measured against.

```

**Targets named, and whether each resolves inside `SCHEMA_SET_FINAL.md`:**

| `PREREG.md` line | operative target? | resolves | how |
|---|---|---|---|
| 1030 | **yes** | **YES** | record `SC-13a`, text at ssf 1282–1343 |
| 1031 | no — explicit non-supersession, stated by the field itself | n/a | field says: "Lines 1031, 1033 and 1035 stand byte-exact and are **NOT superseded**" |
| 1033 | no — explicit non-supersession, stated by the field itself | n/a | field says: "Lines 1031, 1033 and 1035 stand byte-exact and are **NOT superseded**" |
| 1035 | **yes** | **YES** | record `SC-12`, `SC-13b`, `SC-13c-1`, text at ssf 1081–1126; ssf 1376–1446; ssf 1481–1584 |

## SC-13b — INSERTION POINT field at `SCHEMA_SET_FINAL.md` line 1364

**Verbatim:**

```
**INSERTION POINT.** **Pure insertion.** Inserted into §10.2 criterion 2's continuation block
**between line 1035's paragraph and line 1036** (criterion 3), at the block's three-space
indentation, **after** the "waived" definition SC-12 inserts at the same anchor — SC-12 is applied
first, and SC-13b follows SC-12's inserted block. The enumeration is untouched: criterion 3 still
follows the block.

```

**Targets named, and whether each resolves inside `SCHEMA_SET_FINAL.md`:**

| `PREREG.md` line | operative target? | resolves | how |
|---|---|---|---|
| 1035 | **yes** | **YES** | record `SC-12`, `SC-13b`, `SC-13c-1`, text at ssf 1081–1126; ssf 1376–1446; ssf 1481–1584 |
| 1036 | no — boundary | n/a | field says: "between line 1035's paragraph and line 1036 (criterion 3)" |

## SC-13c — INSERTION POINT field at `SCHEMA_SET_FINAL.md` line 1466

**Verbatim:**

```
**INSERTION POINT.** **Two:**

1. **Pure insertion, immediately after SC-13b's block**, same three-space indentation, still between
   line 1035's paragraph and line 1036. The enumeration is untouched.
2. **A pointer paragraph inserted after `PREREG.md` line 816** (§7.2.1), between line 816 and line
   818, so that the site of the excepted rule signals the exception — the same convention H1's hunk
   H8 applies at §7.7 for SC-12's `waived`. Text at §13c-P below. Anchor: line 816 as a full-line
   match (count 1 as read this pass); re-derive after any earlier edit.
```

**Targets named, and whether each resolves inside `SCHEMA_SET_FINAL.md`:**

| `PREREG.md` line | operative target? | resolves | how |
|---|---|---|---|
| 816 | **yes** | **YES** | record `SC-13c-2`, text at ssf 1610–1617 |
| 818 | no — boundary | n/a | field says: "inserted after `PREREG.md` line 816 (§7.2.1), between line 816 and line 818" |
| 1035 | **yes** | **YES** | record `SC-12`, `SC-13b`, `SC-13c-1`, text at ssf 1081–1126; ssf 1376–1446; ssf 1481–1584 |
| 1036 | no — boundary | n/a | field says: "still between line 1035's paragraph and line 1036. The enumeration is untouched" |

---

## RESULT

**16 of 16 fields have every operative target resolving inside `SCHEMA_SET_FINAL.md`.**

**Exclusions, stated with the result (§30.1).** The only lines excluded from "operative target"
are the 12 recorded reads above — each carrying the field's own words and a determination made by
reading, not by pattern. **All 12 were relied on; none fired on nothing.** An exclusion that cannot
be justified is not an exclusion, it is a miss (§30.2).

**There is no classifier.** An earlier version of the check used a `NOT_TARGET` pattern to
separate boundary references from real targets. It was removed, not widened: the vocabulary is
unbounded (C2.1), and the widened pattern was itself corrupted in writing — a `\b` became a
literal BACKSPACE byte, the same defect that gave the §88 sweep two false cleans. See D2 in
`R70_B6_INSTRUMENT_DOMAIN_AUDIT.md`.

