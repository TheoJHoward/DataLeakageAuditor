ITEM H3 — working notes (scratchpad; not a repo artifact)

FILES HERE
  probe.py            existing detectors vs every candidate md file  -> 0 hits on the declaration
  probe2.py           first candidate-detector calibration
  probe3.py           refined detectors + attribution exemption
  probe4.py           final patterns + substring quote exemption     -> 14 hits, all on the declaration
  baseline_stdout.txt --stage prereg BEFORE the change (exit 0, all PASS)
  after_stdout.txt    --stage prereg AFTER  the change (exit 1, single_source FAIL, 14 findings)
  h3_final.diff       the exact diff applied (250 insertions, 15 deletions)

ONLY FILE EDITED IN THE REPO: tools/check_registration.py
  new sha256 30d3ad4cfd72e1cb611bdbc4f3fb72cc66b00a9c1292aa76c1d564b4ceed7425
  PREREG.md untouched (confirmed via git diff --name-only).

KEY EMPIRICAL FACT driving the design
  Widening the file list ALONE finds nothing: the eight pre-existing detectors
  were written against restatements that had appeared in DESIGN.md, and score 0
  on AVAILABILITY_DECLARATION.md (probe.py). Five file-agnostic detectors were
  added, calibrated to fire on no other scanned file.

SCAN SET RESOLVED ON THE CURRENT TREE
  scanned : AVAILABILITY_DECLARATION.md, DESIGN.md, DEVIATIONS.md,
            PRIOR_ART_VERIFICATION.md, README.md
  excluded dirs  : evidence, .git, .pytest_cache, __pycache__
  excluded files : PREREG.md, HISTORY.md, PARKING_LOT.md,
                   tests/registration/EXPECTED_OUTPUTS.md

OPEN ITEM FOR THE PARENT
  tests/registration/test_checker.py:363 test_prereg_stage_on_real_repo_exits_zero
  now fails. It asserts the same thing the tag gate asserts. It is red because
  the scan is now correct, not because the checker is broken. It goes green when
  H2's scrub moves the flagged rules into PREREG.md. The test was NOT edited
  (out of permitted scope for this item).
